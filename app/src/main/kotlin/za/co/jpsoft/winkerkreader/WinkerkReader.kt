package za.co.jpsoft.winkerkreader

import android.app.Application

class WinkerkReader : Application() {

    override fun onCreate() {
        super.onCreate()
        // Initialize any app-wide components (e.g. Timber, StrictMode)
    }
}