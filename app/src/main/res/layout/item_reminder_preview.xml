<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingHorizontal="8dp"
    android:paddingVertical="2dp">

    <!-- Selection dot, wrapped in a 40dp touch target for accessibility.
         The dot itself stays visually small (10dp); tapping anywhere in
         this FrameLayout toggles selection without opening the date picker,
         because Android delivers the click to the innermost clickable view. -->
    <FrameLayout
        android:id="@+id/dot_touch_target"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="?attr/selectableItemBackgroundBorderless">

        <View
            android:id="@+id/view_dot"
            android:layout_width="10dp"
            android:layout_height="10dp"
            android:layout_gravity="center"
            android:background="@drawable/bg_badge_red" />

    </FrameLayout>

    <TextView
        android:id="@+id/tv_preview_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textAppearance="?attr/textAppearanceBodySmall" />

    <!-- Tapping the row (anywhere outside the dot) opens a date picker for
         this reminder. The date text is tinted with colorPrimary as a
         lightweight "this is editable" affordance. -->
    <TextView
        android:id="@+id/tv_preview_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:textAppearance="?attr/textAppearanceLabelSmall"
        android:textColor="?attr/colorPrimary" />

</LinearLayout>