<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_pastoral_item_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingHorizontal="1dp"
    android:paddingVertical="2dp">
    <!-- Congregation colour indicator -->
    <ImageView
        android:id="@+id/widget_pastoral_congregation_indicator"
        android:layout_width="12dp"
        android:layout_height="12dp"
        android:layout_marginEnd="4dp"
        android:src="@drawable/ic_circle" />

    <TextView
        android:id="@+id/widget_pastoral_item_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:ellipsize="end"
        android:singleLine="true"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/widget_pastoral_item_overdue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="#D32F2F"
        android:paddingHorizontal="4dp"
        android:paddingVertical="1dp"
        android:text="@string/bediening_agterstallig"
        android:textColor="#FFFFFF"
        android:textSize="11sp"
        android:visibility="gone" />
</LinearLayout>